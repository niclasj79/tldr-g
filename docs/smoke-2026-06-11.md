# Smoke Report 2026-06-11

Private launch-candidate smoke for the public repo extraction.

## Passed

- Editable source quickstart: `python examples\quickstart.py`.
- Public source tests: `python -m pytest tests\test_public_smoke.py tests\test_water.py tests\test_component_registry.py tests\adapters\test_contracts.py tests\adapters\test_registry.py -q` -> 54 passed.
- Wheel build: `python -m pip wheel . --no-deps --no-cache-dir -w dist`.
- Clean Windows virtualenv wheel install plus quickstart.
- Installed-wheel import resolved from the clean virtualenv `site-packages`, not from the checkout.

## Fixed During Smoke

- Minimal installs could not import `tp_vrg` without spaCy because `extraction.stopword_filter` imported spaCy stopword lists at module import time. The filter now falls back to bundled minimal English/Swedish stopword sets when spaCy is unavailable.
- Minimal installs could not render the quickstart because default PageRank centrality uses SciPy through NetworkX. `scipy` is now a declared core dependency.

## Still Open

- Final public license decision.
- README claim/proof scrub.
- Secret/internal-term scan and source boundary review.
- Windows installer smoke.
- Three outside soft-seed installs.
